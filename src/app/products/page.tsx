import Banner from "@/components/Banner";
import Products from "@/components/Products";

const Home = async () => {
	return (
		<main>
			<Products />
		</main>
	);
};

export default Home;
